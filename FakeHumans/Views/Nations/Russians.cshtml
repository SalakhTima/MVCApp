@model IEnumerable<FakeHumans.Models.Human> 

@{
    ViewBag.Title = "Russians";
}

@foreach (FakeHumans.Models.Human human in Model)
{
    await Html.RenderPartialAsync("~/Views/Shared/_Card.cshtml", human);
} 
